info:
  name: GetUserRepositories
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/users/{{user}}/repos?per_page=10"
  params:
    - name: per_page
      value: "10"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
