info:
  name: GetAuthenticatedUser
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/user"
  auth:
    type: bearer
    token: "{{authToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
